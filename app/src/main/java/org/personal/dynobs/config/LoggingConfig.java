package org.personal.dynobs.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class LoggingConfig {
    // Logging configuration will be handled through application.properties
}
